login:
  vault:
    address: http://vault.example.com:8200
    method: github
    token: ${GITHUB_PERSONAL_ACCESS_TOKEN}

env:
  FOO:
    type: vault
    path: secret/some-path/foo
    field: value
  BAR:
    type: vault
    path: secret/some-path/bar
    field: value
